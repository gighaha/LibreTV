name = "libretv"
compatibility_date = "2024-12-01"

# D1 数据库绑定
# database_id 在创建 D1 后从 Cloudflare Dashboard 获取
[[d1_databases]]
binding = "DB"
database_name = "libretv-history"
database_id = "<替换为你的 D1 database_id>"

# 建表 SQL（首次部署时执行）
# wrangler d1 execute libretv-history --file=./schema.sql
